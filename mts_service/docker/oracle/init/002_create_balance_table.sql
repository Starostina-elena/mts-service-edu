BEGIN
    EXECUTE IMMEDIATE '
    CREATE TABLE APP_USER.BALANCE (
      ID NUMBER GENERATED BY DEFAULT ON NULL AS IDENTITY PRIMARY KEY,
      USER_ID NUMBER NOT NULL,
      AMOUNT NUMBER(19,2) DEFAULT 0 NOT NULL,
      UPDATED_AT TIMESTAMP WITH TIME ZONE DEFAULT SYSTIMESTAMP
    )';
EXCEPTION
    WHEN OTHERS THEN
        IF SQLCODE = -955 THEN
            NULL;
        ELSE
            RAISE;
        END IF;
END;
/

BEGIN
    EXECUTE IMMEDIATE 'CREATE INDEX APP_USER.IDX_BALANCE_USER_ID ON APP_USER.BALANCE(USER_ID)';
EXCEPTION
    WHEN OTHERS THEN
        IF SQLCODE = -955 THEN
            NULL;
        ELSE
            RAISE;
        END IF;
END;
/
