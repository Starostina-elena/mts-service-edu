# Oracle support removed. This Dockerfile was intentionally cleared because Oracle was replaced by a second Postgres instance.
# If you need this back, restore from VCS or the original image: gvenzl/oracle-xe:18-slim

